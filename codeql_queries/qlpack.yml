---
library: false
name: diff-static-analysis-npm-mal-behavior-queries
version: 1.1.0
dependencies:
  # This uses the latest version of the codeql/javascript-all library.
  # You may want to change to a more precise semver string.
  codeql/javascript-all: "*"
